{
  "name": "classroom-studio-firmware",
  "version": "1.2.0",
  "description": "Configure, verify, and safely prepare a Raspberry Pi Pico",
  "main": "main.js",
  "private": true,
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check renderer.js",
    "package": "electron-packager . Classroom-Studio-Firmware --platform=win32 --arch=x64 --overwrite --out=out --ignore='(^|/)(out|node_modules/.cache)(/|$)'"
  },
  "devDependencies": {
    "electron": "38.8.6",
    "electron-packager": "17.1.2"
  }
}
