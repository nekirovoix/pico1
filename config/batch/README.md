# 30 distinct sequential firmware builds
Each configuration has a different institution, product, target board, VID/PID, serial prefix, and drive label. Only `source_board: raspberry_pi_pico` and required runtime settings are shared for hardware compatibility. Random VID/PID pairs are for private classroom testing only.
